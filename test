let rec generateFibonacci(rangeStart, rangeEnd) =  
    rangeStart > rangeEnd -> nil  
  | generateFibonacci(rangeStart + 1, rangeEnd), Print(' '),Print(fibonacci rangeStart)  

where rec fibonacci(n) =  
    n eq 0 -> 0  
  | n eq 1 -> 1  
  | fibonacci(n - 1) + fibonacci(n - 2)  

in  
    generateFibonacci(0, 10)
